# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: trivy

on:
  push:
    branches:
      - main
      - trivy-github-action
  pull_request:
  schedule:
    # every Monday at 7:31pm
    - cron: '31 19 * * 1'

permissions:
  contents: read

jobs:
  build:
    permissions:
      contents: read # for actions/checkout to fetch code
      security-events: write # for github/codeql-action/upload-sarif to upload SARIF results
      actions: read # only required for a private repository by github/codeql-action/upload-sarif to get the Action run status
    
    name: Build
    runs-on: "ubuntu-22.04"
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Check directory
        run : |
          ls -l

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: fs
          scan-ref: '.'
          ignore-unfixed: true
          format: 'sarif'
          output: 'trivy-results-binary.sarif'
          severity: 'CRITICAL,HIGH,MEDIUM'
          exit-code: '1'

      #TODO: use exit-code, or similar above, but still print the report on failure (how?)
      - name: List Issues
        # if: ${{ failure() }}
        run: |
          cat trivy-results-binary.sarif
          exit 1

      #TODO: uncomment when codeql is available
      # - name: Upload Trivy scan results to GitHub Security tab
      #   uses: github/codeql-action/upload-sarif@v2
      #   with:
      #     sarif_file: 'trivy-results-binary.sarif'
